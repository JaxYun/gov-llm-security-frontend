# 排版与界面文案规则

本规则用于避免后续功能再次引入过小字号、过密排版和过度解释。

## 调研依据

- Ant Design 字体规范：https://ant.design/docs/spec/font-cn/
  - 优先使用系统字体栈。
  - 中后台主字号不低于 14px，字重主要使用 400 和 500。
  - 一个系统的字阶尽量限制在 3—5 种。
- Ant Design 排版规范：https://011x.ant.design/docs/spec/typography
  - 提示文案建议不超过 16—18 个字。
  - 每句只表达一个观点，使用明确词语和主动语态。
- Carbon 间距规范：https://carbondesignsystem.com/elements/spacing/overview/
  - 使用 8px 节奏组织区块、内边距和组件间距。
  - 间距用于表达信息关系，不是尽量压缩屏幕。
- Carbon 内容规范：https://carbondesignsystem.com/guidelines/content/writing-style/
  - 界面文案要简单、清晰、直接，删除冗余短语。
  - 不通过大写、特殊名词或多层强调制造“重要感”。

## 本项目强制规则

1. 正文 14px，页面默认 15px，表格、标签、编号和辅助文字不低于 13px。
2. 页面标题 24px，模块标题 17px，关键数字 25—29px。
3. 只使用系统 UI 字体；等宽字体仅用于 ID、接口和数据库字段。
4. 字重以 400 和 600 为主，不使用频繁加粗建立层级。
5. 页面区块之间保留 24px，容器内边距优先使用 16px、24px 或 32px。
6. 页面副标题只说明当前任务，不讲解产品设计原理。
7. 数据限制只在最接近该数据的位置提示一次，不在多个卡片重复说明。
8. 空状态应说明缺少什么和下一步，不解释系统设计意图。
