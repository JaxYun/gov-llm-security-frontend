# 前端严苛审计与 V10 升级记录

## 审计结论

V9 已具备真实任务操作能力，但与成熟的数据安全产品相比仍有六类差距：

1. **结构债务**：主组件同时承担数据请求、状态管理、格式转换和全部页面渲染；CSS 经多轮覆盖后达到 1,300 余行，存在大量已不再使用的规则。
2. **信息架构不稳定**：导航只保存在组件状态中，刷新后无法恢复页面，也无法分享某一功能的直接链接；任务监控在离开运行页面后丢失。
3. **数据操作不完整**：运行、Trace、风险和复核列表缺少统一的搜索、筛选、分页、加载和空状态规范。
4. **视觉系统不统一**：存在多个字号、圆角、间距和状态颜色体系；表格、详情、表单和卡片的边界密度不一致。
5. **交互反馈不足**：缺少统一加载骨架、更新时间、全局刷新、键盘可操作的命令面板、折叠侧栏与运行中全局状态。
6. **响应式策略偏补丁化**：移动端主要依赖隐藏内容和横向滚动，没有系统地重排表单、详情和操作区。

## 参考原则

- Carbon：表格工具栏承载全局搜索和筛选；行悬停、分页和加载骨架属于一致行为。
- Grafana Saga：对象列表采用稳定的“标题—筛选—表头—数据—空态—分页”模板；无数据时保留表头并在原数据空间说明原因。
- Atlassian：使用命名设计令牌统一颜色、间距、字号和状态语义；正文以 14px 为产品默认字号，12px 仅用于辅助信息。
- GOV.UK：有限字号与间距刻度形成可预测的垂直节奏，并使用相对单位保证缩放可读性。

## V10 实施范围

### P0：架构与状态

- 将接口类型、请求、格式化和连接状态拆入 `platform.ts`。
- 将状态标签、空态、分页、骨架屏和标题拆入 `console-ui.tsx`。
- 运行控制提升到应用根层，页面切换不再中断监控。
- URL 保存当前页面，支持刷新恢复、浏览器前进后退和页面直达。

### P0：统一视觉系统

- 删除旧的叠加式 CSS，建立单一设计令牌层。
- 使用中性灰阶、克制的业务蓝和少量语义色。
- 统一 4/6px 圆角、边界、按钮高度、表格行高和表单高度。
- 使用系统中文字体和 rem 字号，避免展示型大标题与装饰卡片。

### P1：核心业务页面

- 工作台改为实际指标、运行表和运行环境侧栏。
- 评测运行改为分区表单、明确外部调用门禁和持续步骤监控。
- Trace 改为可搜索、可筛选、可分页的主从详情结构。
- 风险台账、人工复核、检测器和数据集统一为对象页模板。
- 人工复核增加待处理/已完成双队列，并保留数据库结果。

### P1：产品级细节

- 补充加载骨架、连接异常、无数据、无搜索结果等状态。
- 增加全局刷新时间、运行中状态、侧栏折叠和本地偏好保存。
- 命令面板支持 `⌘/Ctrl + K`、上下方向键、Enter 和 Escape。
- 加入减少动态效果偏好、清晰焦点态、语义标题和可访问标签。
- 重排平板和移动端布局，表单和详情在小屏转为单列。

## 后续仍应推进

- 服务端游标分页与服务端搜索，目前前端只对最近 100 条记录分页。
- 任务取消、超时、重试和断线后的运行恢复。
- 基于用户身份的权限、审计日志和敏感操作审批。
- 正式的目标模型调用链、模型响应查看和工具调用 Trace。
- 对比两个运行、两个检测器版本或两个数据集版本的差异分析。
- E2E 浏览器回归与无障碍自动化检查。
