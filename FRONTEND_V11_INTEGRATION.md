# V11 系统整合升级记录

本轮不再增加展示型分区，而是把前端升级为以“运行”为核心的安全评测控制面。

## 已完成

- 运行上下文贯通：工作台、运行分析、Trace 和人工复核共享运行 ID 与 Trace ID，页面地址可恢复当前定位。
- 基线对比：选择任意两次已完成运行，对比 Trace 数、攻击成功率、防御成功率、人工复核量、判定分布、攻击代次和策略表现。
- 证据下钻：按运行加载最多 1,000 条实际 Trace，从证据详情直接进入对应人工复核记录。
- 标准导出：每次运行可导出包含运行元数据、统计汇总和 Trace 的 JSON，或仅导出 Trace CSV。
- 启动前检查：根据服务连接、数据文件、API key 和本地 Transformers 环境判断任务能否启动；不可用选项会被禁用。
- 数据集状态：本地文件实际存在、Hugging Face 连接器是否安装均由服务端检查，不再统一显示“已配置”。
- 接口整合：新增 `GET /api/runs/{run_id}/analytics` 和 `GET /api/runs/{run_id}/export`。

## 产品方法来源

- 采用实验平台的基线/候选运行比较模型，而不是只显示单次总数。
- 采用可观测平台的“聚合指标 → 运行 → Trace → 复核”逐层下钻结构。
- 采用标注队列的 Trace 直达人工结论工作流，减少跨页面重新搜索。
- 用 URL 保存操作上下文，使浏览器前进、后退和刷新保持可预测。

## 仍需外部输入

- 真实目标大模型的 API 地址、模型名与密钥，或可加载的本地模型目录。
- 正式政务安全数据集及其使用授权。
- 用户、角色、项目隔离和审计要求。
- `GovernmentFactChecker` 的接口协议和事实源。
